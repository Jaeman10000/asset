# 평일편 '수급 브리핑' 형식 — 만들어진 대로 (as-built, 2026-09-16)

JJ 2026-09-15 밤: "매일 내 영상을 보려는 이유는 그날 장의 수급, 돈이 어디로 갔는지를 브리핑하는 게 우선이야. 수급(외국인·기관·개인·기타)을 나열해 보여주고, 돈이 빠졌는지 들어왔는지, 어디에 정체돼 있고 어디로 옮겨 갔는지. 예: 이차전지·로봇에 돈이 들어왔다 → 수급 현황 다 보여주고 → 코스피·코스닥 등락률과 누가 샀는지 빠졌는지 → 이차전지·로봇으로 돈이 들어왔다 → 그 카테고리 안에서도 외국인·기관·개인 → 대장주 1개와 가장 많이 오른 종목 1개, 외국인·개인·기관이 샀는지 팔았는지 → 관련 뉴스가 있는지, 뉴스와 맞물려 오른 건지 → 내일 주시할 포인트."
그리고: **"경제사냥꾼 md 시스템 형식을 무조건 이용하면서 이 판을 이어간다."**

그래서 **장치는 정본(`docs/SCRIPT_SYSTEM_HUNTER_v1.0.md`) 그대로, 내용의 순서는 매일 같은 수급 순서로 고정**한다. 7슬롯을 슬롯대로만 돌렸던 첫 판(헌터, `docs/HUNTER_FORMAT_DESIGN.md`)이 어지러웠던 이유는 S3 체인이 날마다 다른 블록(콜백/이슈/정체…)을 골랐기 때문이다 — 이제 체인은 고정이다. 헌터 7슬롯 구현은 주간 결산편용으로 남겨 둔다.

견본 대본(JJ 확인본): `docs/scripts/2026-09-15_brief.md`. 실제 산출 예: `out/20260915_b1/kr/script.txt`(영상 2:50).

## 0. 원칙
- 장면 id 는 헌터와 같다: `s0 s1 s2 s3a s3b s3c s4 s5 s6`. 대사는 매일 새로(전 편과 글자 같은 문장 0, `script_memory.pick`), 이어지는 사실은 "닷새째·9월 들어 매일·N일째"로.
- 습니다체, 한 문장 숫자 ≤2, 100억 단위(1조 5,700억), 부호·방향을 말한다. 종목 추천·목표주가·전망 금지. "여러분"·자기 영상 언급 금지. 시그니처 고정, 애프터마켓 문장 9/18까지.
- **길이 1,200자(약 165초).** 9/15 실측 1,241자 = 169.6초였다. 쇼츠 한계 180초, 우리 상한 172초에 여유를 둔다.
- 데이터가 없으면 장면을 비우지 않고 §2 대체 규칙으로 채운다. 검사 3회 실패 → `build_aplus` 폴백(영상은 나간다).

## 1. 장면(고정 순서) ↔ 장치 ↔ 데이터
| id | 장치(정본) | 내용(매일 이 순서) | 데이터 | 화면 |
|---|---|---|---|---|
| **s0** | 모순 후킹 | 오늘 수급에서 부딪히는 숫자 둘. 후보 M1~M7(세기 순 M2 장중 스냅 > M1 주인공↔지수 > M3 업종 유출↔등락 > M7 유출↔유입 비율 > M4 이슈 등락↔유입 > M5 > M6). 첫 단어 숫자/주체, 인사·날짜 금지 | investors, intraday, flows, event | S0H |
| **s1** | 단일 질문 선언 | 항상 돈의 행방("오늘 그 돈이 어디로 갔느냐") + 꼬리("이것 하나만 봅니다") | — | S1H |
| **s2** | 대변→차단 + **코스피 수급** | 뻔한 답 → 외국인 → 기관 → 개인을 **넷 다 숫자로**(연속일 포함) → "그런데 [화면 지시어] 네 번째 막대" 기타법인 + 상위 2종목·자사주 비중·"9월 들어 매일/N일째" → 다음 질문 | investors.kospi, inv_streak, top_others, continuity | S2H(막대 4개 고정 순서) |
| **s3a** | 데이터 블록 1 + 콜백 | **코스닥 3주체(개인까지) + 지수 둘** + 한 줄 판정 + **어제 보자고 한 숫자 회수**(도장 이어짐/끊김, 월요일은 주말편 것도) → 질문 | investors.kosdaq, kospi/kosdaq, callback, weekend_watch | S3aB |
| **s3b** | 데이터 블록 2 | **돈이 빠진 곳·정체된 곳**: 유출 1위 업종(외/기 분리, N일째) + 대장주 등락 + **자사주 받침** + 나머지 유출 | flows.table_t(<0), flow_day, stock_flows, others_top | S3bB |
| **s3c** | 데이터 블록 3 + 이동 선언 | **돈이 들어온 곳**: 유입 상위 2 업종(외/기 분리, 등락, 종목 이름) + 유출 1위 대비 비율 → 질문 | flows.table_t(>0) | S3cB |
| **s4** | 1차 자료 한 칸 | "직접 열어봤습니다, 키움 종목별 투자자 표, M월 D일 마감 기준" → **대장주 1 + 최대 상승 1**: 등락·외국인·기관·**개인**·거래대금 + 검산된 계산 1개 | raw/brief_stocks.json | S4B(종목 표) |
| **s5** | 양면 판정 + 조건 + 콜백 + 한계 | **뉴스와 맞물렸나**: 뉴스 ≤2 → "뉴스가 올린 값이면 큰손 돈이 작다 / 돈이 올린 값이면 외국인·기관이 같이 산다" → 업종별 판정 → 뒤집히는 조건 하나(임계값) | news_kr.json, events.json, brief_stocks | S5B |
| **s6** | 내일 관측값 + 시각 + 시그니처 | 2~3개(외국인 N+1일째 / 유입 업종 이틀째 / 기타법인 선) + 다음 이벤트 + 애프터마켓 + 시그니처 | inv_streak, flows, schedule | S6H |

문장 규칙 10·체크리스트 11항은 그대로 검사한다. 블록(s3a·s3b·s3c) 끝은 "그런데" 문장 또는 질문, 화면 지시어 ≥4, "그런데" 블록당 1회.

### 1-1. 길이 예산에서 **절대 빼지 않는 칸** (JJ 요구)
`GLOBAL_DROP` 에 넣지 않는다 — 코스닥 개인(`s3a kosdaq_2`), 종목별 개인·거래대금(`s4 row:0_3`, `row:1_3`), 둘째 유입 업종 수급(`s3c t2`), 자사주 받침(`s3b support`), 다음 이벤트(`s6 event`). "외국인 개인 기관 이것도 샀는지 팔았는지 알려주고" 가 이 형식의 이유다.

## 2. 대체 규칙
- 유입 업종 없음: s3c "들어온 곳이 없었습니다. 가장 덜 빠진 곳은 …", s4 는 유출 1위 업종 대장주 + 가장 덜 내린 종목, s5 는 유출 업종 뉴스.
- 이슈(events.json) 없음: news_kr.json 제목에 업종·종목 이름이 든 기사만. 없으면 "뉴스 없이 수급만 움직인 날" + 판정은 돈 쪽.
- 기타법인 <3,000억: 자사주 문장 생략, "기타법인 N억" 한 마디.
- 코스닥 수급 없음: s3a 는 지수 둘 + 코스피 판정 + 콜백.
- 어제 약속 없음: 콜백 문장 생략, 외국인 연속일 사실로.
- brief_stocks 수집 실패: s4 는 flow_day 의 외/기·등락·거래대금만(개인은 말하지 않음, `no_indiv` 표시), 계산은 외+기 ÷ 거래대금.
- 오후 2시 스냅 없음 / intraday 가 16시 이후 수집: M2 훅 제외.

## 3. 파일
| 파일 | 역할 |
|---|---|
| `jobs/narrate_brief.py` | `build(c, avoid=None, attempt=0)` — 고정 체인 생성기. 후보 6벌 이상, `script_memory.pick`(문장 단위 회피, offset=attempt), `continuity()` 로 이어짐 문장, 낱말 반복(오늘·막대·칸·쪽 ≤3) 벌점, 1,200자 예산(`GLOBAL_DROP` → `compact` 재조립) |
| `jobs/collect_brief.py` | 유입 1위 업종의 대장주(flow_store sectors[].leader → 거래대금 최대)와 최대 상승 종목 → ka10059 당일 외국인·기관·개인·거래대금 → `raw/brief_stocks.json`. 실패해도 `{}` 저장·예외 없음 |
| `jobs/brief_try.py` | 오프라인 시험 `DATE… [--cross] [--attempt N] [--selftest]` — data/·out/ 미접촉 |
| `jobs/qa_script.py check_brief` | 체크리스트 11항 + 문장 규칙 + 브리핑 필수 칸(4주체·지수·유출·유입·종목 셋·뉴스·임계값) + 전 편 겹침(창 5·올라간 편·장부 문장 예외) + 1,200자 |
| `jobs/script_memory.py` | 전 편 문장·사실 색인. `pick`(문장 단위), `overlaps`, `continuity`(가장 산 쪽·기타법인·주인공·업종·약속 연속일), `record_history` |
| `render/src/v4/BriefV4.tsx` | s3a(코스닥+지수+도장) s3b(유출 막대+종목 칩+자사주) s3c(유입 두 색 막대+종목 이름+비율) s4(종목 표+계산) s5(뉴스+양면 판정). s0·s1·s2·s6 은 HunterV4 재사용 |
| `jobs/make_thumb_auto.py` | 훅의 두 숫자로 썸네일 세 줄. review 단계에서 매일 자동 |
| `data/publish_config.json` | `script_format: "brief"`, `mode: "manual"`(자동 게시 끔 — 업로드는 크롬으로 사람 손처럼 직접) |

## 4. 하루 흐름 (월~금)
14:00 장중 스냅(`intraday`) → **15:31 시작, 15:40:45 까지 기다렸다 수집+대본**(키움 종목별·업종 수급은 15:40 전엔 확정 전 값 — 9/17 사고. `jobs/tasks/evening_collect.ps1`: collect 약 8분 = 업종 80초·코스피 전 종목 127초·코스닥 전 종목 240초 → compute: 뉴스 → 이슈 → collect_brief → 뉴스 추가 → 브리핑 대본, 실패 3회면 aplus 폴백) →
**≈15:50 대본을 JJ에게 먼저 보낸다**(Claude 예약 작업 `nugasatna-script-preview` 가 15:40 에 깨어나 15:58 까지 대본을 기다린다, 앱이 켜져 있어야 돈다) →
**JJ가 OK 하면 Claude 세션이 `data/<날짜>/APPROVED` 를 만든다** → 15:55부터 16:45까지 그 파일을 기다리던 제작 작업이 음성·영상·검토·썸네일(`jobs/tasks/evening_build.ps1`: tts → render → review — **대본을 다시 뽑지 않는다**, OK 없으면 안 만든다. 다시 뽑으면 그사이 늘어난 뉴스로 JJ가 본 대본과 영상 대사가 달라진다) →
17:00 유튜브 예약 공개·Threads(크롬으로 직접). JJ가 대본을 고치라고 하면 compute 를 손으로 다시 돌리고 tts·render·review 를 다시 한다. **세션이 손으로 음성·렌더를 돌릴 땐 `data/<날짜>/MANUAL` 을 먼저 만든다**(9/18 렌더 겹침 — 자동 제작이 APPROVED 를 보고 같이 돌았다). 음성·렌더는 `run_day.py` 잠금으로 한 번에 하나만 돈다.
JJ가 문장을 직접 고쳐 확정하면 `data/<날짜>/script_edit.json`(pairs·hunter·jj_final) 에 넣고 compute 를 다시 돌린다 — 16시가 지났으면 `compute.compute(d)` 만(수집 없이).
산출물: `out/<날짜>/kr/` 의 `script_preview.txt(≈15:50) · video.mp4 · script.txt · youtube_title.txt · youtube_description.txt · youtube_tags.txt · thumb_A.jpg · threads.txt · card.png`.

## 5. 검증 방법
- `python brief_try.py 20260915` — check_brief 통과, ≤1,200자, §1-1 칸이 전부 있는지.
- `python brief_try.py 20260911 20260914 20260915 --cross` — 세 편 통과, 글자 그대로 겹침 0.
- `python brief_try.py --selftest` — §2 대체 규칙 13케이스.
- `python test_qa_brief.py`, `python test_qa_hunter.py`, `python script_memory.py --selftest`.
- 샘플 영상: `data/<날짜>_b1/` 로 복사해 `run_day.py <날짜>_b1 --stage=tts|render|review`. **ledger.json 은 미리 백업하고 끝나면 복원**(compute 를 돌릴 때만 해당).

## 6. 변경 이력
- **2026-09-19 밤** — 평일편 자막을 **가운데 검은 둥근 상자**로(`ScenesV4.Shell`: bottom 240, 배경 rgba(8,10,16,0.9), 42px, 둥근 모서리 18) — 정보 영상 자막 모양에 맞춤(JJ "국장·주간 자막이 허접"). 토요일(`WeeklyV4`·`V5`)·일요일(`WeeklyUSV4.UShell`)도 같은 상자. 9/18 편 25장면 스틸로 겹침 없음 확인. 음성은 '누가샀나'를 '누가 샀나'로 띄어 읽는다(`tts.speakable`, 화면 글자는 그대로 — 붙이면 '누가샜나'로 들렸다). 손으로 쓴 대본의 말하는 % 검사 `_common.pct_speech_issues`(5.01% → 5%). 규칙 전부는 `SCRIPT_GUIDE.md §18`.
- **2026-09-19 — 9/18 편 조회수 28(18시간) 진단.** 스튜디오(크롬 `?authuser=2` 계정이 채널 권한) 실측:
  | 편 | 조회 | 유효 조회 | 계속 시청 | 평균 시청 |
  |---|---|---|---|---|
  | 9/8 | 32 | 8 | 25.0% | 0:35 |
  | 9/11 | 1,242 | 536 | 46.6% | 1:08 |
  | 9/14 | 112 | 55 | 55.7% | 1:29 |
  | 9/15 | 1,340 | 593 | 48.9% | 1:03 |
  | 9/16 | 1,605 | 759 | 52.5% | 1:59 |
  | 9/17 | 1,284 | 555 | 46.1% | 1:39 |
  | 9/18 | 28 | 12 | 52.2% | 2:04 |
  - 9/18 을 본 사람의 반응(계속 시청·평균 시청)은 잘 된 날과 같거나 더 좋다 → **대사·훅 탓이 아니다.** 차이는 노출 수: 잘 된 날은 첫날 안에 쇼츠 피드에서 한꺼번에 1,000회대가 붙고(9/17 누적 곡선) 그 뒤 거의 멈춘다. 9/18 은 그 몰림이 아예 없었다(쇼츠 피드 89% = 25회). **정정(9/19 15:25)**: 몰림이 '3시간 안'은 아니다 — 주간편은 공개 3시간 26분에 66회였는데 스튜디오 대시보드 '조회수 순위'가 최근 10편 중 4위였다(6편은 그 시점에 66회보다 적었다가 나중에 1,000회대가 됐다). 몰리는 시각은 편마다 다르다. 채널 상태: 설정 › 기능 사용 자격 요건 표준·중급·고급 모두 '사용 설정됨'(경고로 막힌 것 없음), 콘텐츠 목록 제한사항 '—'.
  - 파일·제목·태그·길이·BGM·공개 방식·요일로는 저조 편(9/7·9/8·9/14·9/18)과 정상 편이 갈리지 않는다(9/19 워크플로 네 관점 조사 + 반박 검증, 살아남은 가설 0).
  - 확인된 결함 둘을 고쳤다. ① **첫 프레임이 배경뿐**(9/14~9/18 전부, 글자는 0.45~0.75초에 떠오름) → `HunterV4.S0H` 첫 A 글자를 0초부터 보이게. 계속 시청이 모든 편에서 약 50%라 첫 1초가 가장 싸게 올릴 수 있는 곳(원인이라는 증거는 아님). ② **렌더 겹침**: 세션이 APPROVED 를 만들고 손으로 음성·렌더를 돌리는 동안 `evening_build.ps1` 도 APPROVED 를 보고 같이 돌았다(9/18 16:24·16:24·16:31 시작, 한 파일에 덮어씀). → `run_day.py` 음성·렌더 잠금(`data/<날짜>/.produce_<ed>.lock`, 주인 PID 가 죽었거나 30분 지나면 넘겨받음) + **세션이 손으로 제작하는 날은 `data/<날짜>/MANUAL` 을 먼저 만든다**(evening_build 가 보고 손을 뗀다).
  - 참고: 세로 쇼츠 피드 썸네일(oar2)은 6편 모두 영상 중간 자동 프레임이다. 올린 thumb 이미지는 16:9 자리(검색·채널 동영상 탭)에만 쓰인다.
  - 다음에 볼 것: 월요일 편부터 계속 시청 비율이 50%에서 올라가는지. 올린 영상마다 `out/<날짜>/kr/publish.json`(video_id·올린 제목·바꾼 제목과 시각)을 남긴다.
- **2026-09-18** — 9/18 편 제작·업로드(유튜브 `hb_h3dnDqCU` 17:00 예약, Threads `Dda9cOmNpfx` + 답글 `Dda9lssH7av`). 흐름: 15:07 장중 미리 조사(`data/20260918/prep_intraday.md`) → 15:49 자동 대본 → 자동판 s5 "큰 뉴스가 없었다"(실제로는 미 반도체 반등·일본 금리 인상)·"1625%"·"월요일 외국인이 사는 쪽으로 돌아서야"(이미 샀다) 오류 → JJ·GPT 지적 세 번 반영해 `script_edit.json`(jj_final) 확정 → 16:23 APPROVED. 업종 아카이브에 없는 전선·광통신은 ka10059 종목별 확정 수급을 손으로 받아 `hunter.s4.stocks`·`s5.verdicts` 에 넣었다.
  - 화면: `BriefV4.S5B` 판정 칩 2 → 4(양쪽 칸에 종목 둘씩), 칸 설명 `h.a_sub`·`h.b_sub`(없으면 옛 문구), 조건 카드는 `h.condition` 이 있을 때만. `S4B` 표 칸 글자 크기를 가장 긴 칸 글자 수로 줄임(1조 넘는 +13,272 가 38px 로 겹쳤다). s3a 에 말하지 않은 정리 카드(`verdict`)·s3b 말하지 않은 NAVER·카카오 칩·s3c '1625%' 비율 칩은 `hunter` 덮어쓰기로 뺐다.
  - 썸네일: `make_thumb_auto` 가 "미국 금리 인상 / 빠졌는데 / 2.6%만 올랐다?!"(틀린 문구)를 뽑아 `data/20260918/thumbs.json` 을 손으로 써서 다시 그렸다 — 금리 훅(MF)이 아닌 날 대체 규칙 틀 문제, 숙제.
- **2026-09-17 밤 3** — `narrate_brief._bond`: 채널의 생각 한 줄(JJ "숫자와 친해져야 합니다…"). 전 편(script_history)에 표지('숫자와 친해'·'기분이나 감'·'감으로 사고팔'·'기분은 매일 바뀌')가 있으면 쉰다. 넣는 자리는 '실제로 들어온' 판정 뒤, 없으면 flow 뒤. GLOBAL_DROP 에는 없고 마지막 수단 첫째. 9/11·9/14 자동 생성에서 나옴, 셀프테스트·test 50 통과.
- **2026-09-17 밤 2** — JJ 결정 반영. ① s1 꼬리 `_Q1_FIXED` 고정 + 최근 3편이 모두 고정이면 `_Q1_ALT`("과연 누가 샀을까요?" 등), 고정 문장(script_memory.SIGNATURE)에 등록. ② 자사주: `data/buybacks.json` 에 progress(9/16 기준 진행률, 조기 종료 추정 10/1·10/16, 출처) — `_bb_period` 가 원래 끝 달·조기 종료 달(초·중순·말)을 만들고, 기타법인 3일째(또는 어제 자사주를 말한 날)면 s3b support = "자사주 매입은 원래 11월까지입니다", support_2 = "…지금 속도면 … 끝난다는 분석이 나왔습니다"(값이 밀린 날에도). 화면: 기타법인 막대 + '자사주 매입 기간' 카드(원래 날짜 취소선 → 조기 종료 분석). ③ collect_brief role '대장주' → '외국인·기관 최다'(아카이브 leader) / '거래대금 최대', s4 문장 "조선에서 외국인과 기관이 가장 많이 산 …". ④ s5 이슈 없는 날 `_big_mover` — 제목 낭독 뉴스는 버리고 "오늘은 큰 이슈가 없었습니다. 대신 X가 N% 올랐습니다" + 외국인·기관·개인 풀이. ⑤ 오른 이유 제목 필터(값 움직임 말 필수, 제재·소송 등 제외), '이었/였' 조사, 마지막 수단 드롭(macro_link → support_2). 셀프테스트·test 50·9/11~9/17 자동 생성 통과.
- **2026-09-17 밤** — 다음 날 차별화. ① `collect_macro.fomc_last_night`: 간밤이 FOMC 발표일(`collect_us.FOMC_2026`)이 아니면 금리 훅·문장을 만들지 않는다(9/18 아침 기사에도 '금리 인상'이 남아 훅이 또 붙을 뻔했다). ② s2 `size`(어제 대비 ±20% 이상이면 "어제보다 9,300억 줄었습니다") · `week`(이번 주 같은 부호일 때 "5일 가운데 이번 주 가장 적게 팔았습니다" — 누적 합은 토요일 주간 결산과 겹쳐 말하지 않는다). ③ s3b `support` 은어·등식 제거("그 물량은 자사주가 받쳤다" → "두 종목에선 기타법인이 …을 샀습니다") + `support_2` "두 회사 모두 (11월까지) 자사주를 사들이는 기간입니다"(`data/buybacks.json`). ⑥ s2 산 쪽이 셋이면 한 칸에 두 문장(9/17 기관 누락 폴백 원인). S4_OPEN·S5_VERDICT 후보를 넓혀 여는 말·판정 틀이 3일째 같지 않게. ④ 내일 볼 것: 같은 날 시작한 둘째 유입 업종을 묶어 "조선·방산 순매수가 2일째 이어지는지", `_verify_watch` 가 둘 다 판정(섞이면 "조선만 이어졌습니다. 조선 520억 순매수, 방산 180억 순매도"). ⑤ "오늘 주요 이슈를 보겠습니다"를 고정 문장(겹침 검사 예외)에. 셀프테스트·test_qa_brief 통과. 9/18 가상 대본 세 가지는 `SCRIPT_GUIDE.md §3-3`.
- **2026-09-17 저녁** — 9/17 편 제작·업로드. 사고와 고친 것:
  ① 15:31 수집이 확정 전 수급(외국인 −1,022억·개인 0)으로 브리핑 검사에 떨어져 **폴백 대본(590자)이 JJ에게 갔다**. 원인: 키움 종목별 투자자·업종 수급은 15:40 까지 확정 전이고 flow_collector 는 그 시간엔 저장조차 안 한다. → run_day collect 가 15:40:45 까지 기다리고, compute 는 업종 수급이 없으면 다시 받는다. 예약 작업은 15:58 까지 기다린다.
  ② 업종·종목 뉴스 쿼리(오른 이유)는 그날 08:00 부터 본다 — 특징주·리포트는 장중에 나온다(15:20 창에 안 걸렸다).
  ③ s5 금융 문장: 외국인·기관이 엇갈리면 합계(+141)로 "외국인과 기관이 샀다"고 하지 않는다 → "기관이 사고, 외국인은 팔았습니다".
  ④ JJ 확정 대본 장치 `data/<날짜>/script_edit.json`: `pairs`(장면별 [태그, 문장]), `hunter`(화면 데이터), `jj_final: true`(검사 실패를 경고로 남기고 폴백하지 않음).
  ⑤ 16시 뒤 compute 를 다시 돌리면 ka10059 등락에 애프터마켓 체결이 섞인다(삼성중공업 6.44 → 6.19) → `collect_brief` 가 15:40 업종 아카이브의 정규장 `ret` 로 덮는다.
  ⑥ BriefV4 S5B 에 간밤 미국 금리 카드(`hunter.s5.macro`) — 1차본은 1:57~2:05 금리 문장 동안 배경뿐이었다(JJ 지적). 1장면 질문 글자·6장면 말 안 한 옵션 만기 카드도 고쳐 다시 렌더.
  ⑦ 검증 에이전트 둘(사실 대조·흐름/규칙)을 대본 발송 전에 돌렸다 — '대장주' 정의(외국인+기관 최다 순매수 = leader)가 시청자 뜻(시총 1위)과 다르다는 것, 애프터마켓 등락, 자사주 조기 공개로 반전이 약해지는 것 등을 잡았다.
  ⑧ 제목·태그·해시태그는 그날 키워드 조사(구글뉴스 오늘 제목 빈도·유튜브 자동완성·다음 금융 인기검색)로. 유튜브 17:00 예약 `ELDY0Lq_EBk`, Threads 본문+첫 답글.
- **2026-09-17 새벽** — 대본을 이야기 흐름으로 전면 손질. 규칙·전후 예시·남은 숙제는 `docs/SCRIPT_GUIDE.md`(최신 정본). 길이 상한 1,250자.
- **2026-09-17 00:00** — ① 화면을 말로 설명하지 않는다: 검사기 규칙 6 을 브리핑에서 '지시어 4회 요구'→'금지'로 뒤집고(따옴표 속 뉴스 제목 제외), 생성기가 후보를 고르기 전에 거른다. ② s5 판정·흐름·뒤집히는 조건을 사람 말로("반도체는 뉴스보다 돈이 움직인 하루였습니다", "아직은 나가는 돈이 더 큽니다"). ③ 외국인·기관이 반대로 간 업종은 합계를 말하지 않는다. ④ 글씨만 뜬 화면 3초 이내(질문 화면 캡 + s1 에 코스피 장중 흐름 그림). ⑤ 영상 제작은 JJ OK(APPROVED) 뒤에.
- **2026-09-16 22:40** — JJ: "대본을 최대한 빨리 먼저 보여줘, 3시 40분 정도에." 수집을 15:41 → 15:31 로 당기고 수집 직후 대본까지 만든다. 15:40 예약 작업이 대본을 JJ에게 보내고, 15:55 제작은 대본을 다시 뽑지 않고 그 대본으로 음성·영상만 만든다.
- **2026-09-16 18:10** — JJ 지적 셋을 고쳤다. ① 어제 숙제의 답에서 화면 지시어를 말로 설명하지 않는다("…이어짐, 오른쪽 도장입니다" → "어제 보자고 한 외국인 순매도는 오늘도 이어졌습니다") — 화면에 이미 도장이 찍히는데 말로 또 읽으면 기계처럼 들린다.
  ② 유출 2·3위 업종(s3b others)을 예산에서 빼지 않는다 — 1위만 말하면 "왜 반도체만 말해?"가 된다. 자리는 '그런데' 앞으로 옮겼다(블록은 반전·질문으로 닫아야 한다).
  ③ 합계 유입이 0인 날 "들어온 곳이 없었습니다"로 끝내지 않는다 — 한쪽이 전 업종에서 팔면 합계는 전부 마이너스가 되지만 **받은 손**은 따로 있다.
  기관(또는 순매수 쪽)이 가장 많이 받은 업종 둘을 유출 1위를 빼고 말한다. 9/16: "기관이 금융 810억, 이차전지 550억을 받았습니다."
  덤으로 s4 계산이 어느 종목의 거래대금인지 못 박게 했다("이오테크닉스 거래대금 390억 가운데 기관 몫이 26%").
  계산 1칸은 GLOBAL_DROP 끝에서 두 번째라 사실이 꽉 찬 날엔 밀린다 — JJ 요구 칸이 먼저다(brief_try 셀프테스트도 그렇게 고쳤다).
- **2026-09-16 17:05** — 오늘 편 업로드 완료(유튜브 17:00 예약 공개 `cK_vtak6B90`, Threads 본문+첫 답글). 제작이 15:55에 죽은 원인은 `polish.py` f-string 문법 오류였고, 되살린 뒤 사실 오류 셋(반도체 값 방향, s5 근거, 썸네일 M2 앞뒤)을 고쳐 16:35에 다시 뽑았다. 업로드 절차와 함정은 §7.
- **2026-09-16 09:20** — 이번 주 업로드는 JJ가 직접(`mode: manual`). 썸네일 자동 생성 붙임. 길이 1,250 → 1,200자. BriefV4 유입 막대가 도입 문장에 먼저 켜지던 것 수정.
- **2026-09-16 01:40** — 군더더기 문장 정리(150자 절약)로 로봇 수급·자사주 받침·다음 이벤트·양면 프레임 두 쪽·로봇 판정을 모두 넣음. 코스닥 개인·종목별 개인을 예산 제외 칸으로 고정. 9/15 샘플 영상(2:50) 확인.
- **2026-09-16 00:30** — 브리핑 형식 1차 완성(narrate_brief·collect_brief·BriefV4·check_brief). 9/11·9/14·9/15 통과, 5일 연쇄 폴백 0, 조사 오류 0.
- **2026-09-15 23:00** — JJ 판정: 헌터 7슬롯은 "어지럽고 정신없다" → 평일은 수급 브리핑, 헌터는 주간용 보관. 장치는 정본 그대로 쓰라는 조건.
- **2026-09-15 21:00** — 헌터 포맷 리뷰 반영(100억 정밀도, 이어짐 문장, 재시도 회전, 조사·화면 수정).
- **2026-09-15 저녁** — 경제사냥꾼 PDF v1.0 을 `docs/SCRIPT_SYSTEM_HUNTER_v1.0.md` 로 저장, 루트 `CLAUDE.md` 가 매 세션 먼저 읽게 함. 전 편 기억 `script_memory.py` 신설(9/11·9/14·9/15 가 "바깥에서 새 돈이 들어온 게 아니라…"를 그대로 반복한 것이 근거).

## 7. 업로드 절차 (크롬, 2026-09-16 실측)
브라우저 자동화가 걸려 넘어진 자리를 그대로 적어 둔다. 매일 같은 함정이다.

### 유튜브
1. `studio.youtube.com/channel/<채널ID>/videos/upload?d=ud` → `find "file input"` → `file_upload` 로 `video.mp4`(10MB 한도).
2. **좌표 클릭은 믿지 않는다.** 이 크롬은 페이지 확대가 걸려 있어 스크린샷 좌표와 CSS 좌표가 약 1.11배 어긋난다. 어떤 버튼은 눌리고 어떤 버튼은 안 눌린다.
   - 제목: 제목 칸을 **triple_click** 해서 기존 "video" 를 잡고 그 자리에 type. 그래도 뒤에 "video" 가 남으면 `Delete` 를 6번.
   - 설명: 칸을 클릭하고 type. `Input.insertText` 가 30초 타임아웃을 내도 **글자는 들어가 있다** — 다시 넣지 말고 화면으로 확인부터.
   - 버튼(`다음`·`저장`·`예약`)은 `find` 로 ref 를 받아 ref 로 누른다.
3. 썸네일: `find "file input"` → `file_upload thumb_A.jpg`.
4. 아동용 "아니요"는 대개 이미 찍혀 있다. 태그(고급 설정)는 펼침이 안 먹는 날이 있다 — 못 넣어도 올린다.
5. 공개 상태 → `예약` 펼침(`Click to expand` ref) → 날짜 칸의 달력 버튼 → **오늘 날짜 칸은 shadow DOM 안이라 find 가 못 찾는다**. `javascript_tool` 로 `calendar-day today` 를 찾아 pointer/mouse 이벤트를 쏜다.
6. 시간 칸을 triple_click 하고 `오후 5:00` 을 type → `Return` → `예약`.
7. **다이얼로그 밖을 클릭하면 창이 닫히고 초안이 사라진다.** 9/16 1차 업로드가 이렇게 날아갔다.

8. **(9/17 실측) 글자는 JS 로 넣는 게 가장 확실하다.**
   - 제목: `ytcp-uploads-dialog #textbox[contenteditable]` 첫째 칸에 `focus → execCommand('selectAll') → execCommand('insertText', 제목)`.
   - 설명(둘째 칸)·태그(`input[aria-label=태그]`): `DataTransfer` 에 `text/plain` 을 실어 `ClipboardEvent('paste')` — 줄바꿈이 살고, 태그는 쉼표로 나뉘어 칩이 된다.
   - 아동용 아님: `tp-yt-paper-radio-button[name=VIDEO_MADE_FOR_KIDS_NOT_MFK]`, 고급 설정 펼침: `#toggle-button`, 다음: `#next-button` 세 번.
   - 예약: `find "예약 section expand button"` ref 클릭 → 시간 칸을 JS 로 클릭한 뒤 `오후 5:00` 옵션(`tp-yt-paper-item`)에 pointer/mouse 이벤트 → 날짜는 달력을 연 뒤 **스크린샷 좌표로 17 칸을 클릭**(JS 이벤트는 헤더만 바뀌고 되돌아갔다) → 스케줄러 글자에 오늘 날짜가 있는지 확인하고 `#done-button`('예약').
   - 결과 창 "동영상 예약됨 · 2026년 9월 17일 17:00" 과 `youtube.com/shorts/<id>` 링크를 읽어 둔다.
9. JJ가 "먼저 볼게"라고 하면 공개 상태 화면에서 멈춘다(게시·예약 안 누름). 영상 파일은 SendUserFile 로 보낸다.
10. **(9/19 밤 실측 — 9/20 세 편 예약)**
   - 제목·설명: 제목 칸을 **한 번 실제로 클릭한 뒤** JS `execCommand('insertText')`(클릭 전엔 들어가지 않았다) → `innerText.length` 로 확인. computer type 으로 넣으면 제목이 설명 칸으로 새거나 'a' 한 글자가 붙었다.
   - 태그: JS 로 `input[aria-label=태그]` 에 focus → type "a,b,c," → `ytcp-chip` 글자로 확인.
   - **예약 시각은 타이핑하지 않는다.** '오후 12:00'을 type + Return 했는데 값이 '오전 12:00'(자정)으로 남았다. 시간 칸을 클릭해 목록을 연 뒤 `tp-yt-paper-item` 중 글자가 정확히 '오후 12:00' 인 것을 JS click → 값 확인. 밤에 올리면 날짜는 이미 다음 날로 잡혀 있다 — 날짜 글자도 확인.
   - `#done-button`('예약') 뒤 `studio.youtube.com/video/<id>/edit` 에서 공개 상태를 열어 날짜·시각, 썸네일, 태그(자세히 보기)를 다시 본다.
   - **크롬 연결이 끊기면**(탭 그룹이 사라짐) 업로드는 초안으로 남는다(목록에 '업로드 다시 시작·초안 수정'). '초안 수정'으로 이어 가면 제목·설명은 저장돼 있지만 **썸네일은 날아갔다** — 세부정보 페이지에서 다시 올리고 `저장`. 초안은 지우지 않는다.
   - 스크린샷이 30초 타임아웃·겹친 그림으로 나오는 때가 있다(렌더러 멈춤) — 다시 찍거나 JS 로 값만 읽는다.
   - **(9/20 새벽) 새벽에 올리면 예약 날짜가 내일로 잡힌다.** 달력 칸을 스크린샷 좌표로 누르면 빗나간다(이 크롬은 CSS 좌표가 스크린샷의 약 1.34배). 달력을 연 뒤 JS 로 그림자 DOM 을 뒤져 `calendar-day` 중 클래스에 `today` 가 있는 칸(또는 원하는 날)에 pointerdown·mousedown·pointerup·mouseup·click 을 쏜다 — 글자 '20'만 보고 고르면 11/20 같은 다른 달 칸이 먼저 잡힌다. 시간은 목록에서 먼저 고르고 날짜를 바꾼다.
   - 제목·설명 칸: `execCommand('insertText')` 는 칸에 진짜 커서가 있을 때만 먹는다(ref 클릭이 빗나가면 false) — 안 먹으면 칸을 triple_click 해서 선택된 걸 확인하고 computer type.
   - 공개 전 예약본을 다른 파일로 바꿀 때: 새로 올려 같은 시각 예약 → 옛 예약본은 공개 상태 '저장 또는 게시' 펼쳐 비공개 → 완료 → 저장(삭제 안 함).

### Threads
1. 프로필 → 작성 칸 클릭.
2. **type 으로 여러 줄을 넣으면 줄바꿈이 전부 사라진다**(Lexical 에디터). `Return` 키도 안 먹는다. `javascript_tool` 로 `ClipboardEvent('paste')` 에 `text/plain` 을 실어 보내면 줄바꿈이 산다.
3. 한글 type 은 받침이 가끔 깨진다("나눠"→"나눴", "너넨"→"너넘"). 넣은 뒤 `el.innerText` 로 반드시 읽어 본다.
4. **다시 붙여넣기는 덧붙는다.** 앞의 글을 지우려면 취소 → "저장 안 함" → 새 창에서 한 번에 붙인다.
5. **(9/18 부터) 이미지는 붙는다.** 작성 창을 연 뒤 `find "file input for attaching media in the compose dialog"` → `file_upload` 로 jpg 한 장(9/18 thumb_A_v2.jpg). 9/16~17 엔 안 붙었다 — 안 붙으면 본문만 올리고 JJ에게 알린다. 작성 창은 프로필의 '새로운 소식이 있나요?' 버튼으로 열리지 않는 날이 있다 → 같은 줄 오른쪽 '게시' 버튼을 누르면 열린다.
5-1. **(9/18) 로그아웃돼 있을 수 있다**(오른쪽에 '사용자 이름으로 로그인'). JJ에게 로그인을 부탁한다. 계정은 @bearpicks_official(프로필 이름 '누가샀나').
5-2. 글은 `SCRIPT_GUIDE.md §10-1` 틀(부르는 첫 마디·감정 한 마디·발견 한 줄·반복 없는 나열·열린 질문)로 다시 쓰고 JJ에게 먼저 보여 준다. 첫 답글 = "영상으로 정리해 뒀어 → 링크".
6. 게시 버튼은 `게시` 글자를 가진 `div[role=button]` 중 **마지막 것**. 답글은 답글 아이콘(`svg[aria-label=답글]`)을 눌러 칸을 연 뒤 같은 방법으로 붙이고, 오른쪽 아래 동그란 화살표 버튼으로 보낸다.
7. **(9/17)** 본문 끝의 `#국장` 을 붙여 넣으면 주제 제안이 뜬다 → '+ 새로운 주제 태그하기'(국장) 를 고르면 글에서 해시태그가 빠지고 머리에 '> 국장' 주제가 붙는다. 9/16 글과 같은 모양.
8. **(9/17)** 게시 뒤 첫 답글: 프로필 글의 `답글` 버튼(find ref) → 글 페이지의 '…님에게 답글 남기기' 칸 클릭 → 같은 paste → `답글` 동그란 화살표 버튼. 유튜브 링크는 카드로 붙는다. 프로필 '답글' 탭엔 자기 글 답글이 안 보인다 — 프로필 글 목록에서 1/2 · 2/2 로 확인.
