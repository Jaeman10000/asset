# ── Node / 프론트엔드 ──
node_modules/
vitality-nexus/dist/
vitality-nexus/scripts/shots/
vitality-nexus/scripts/*.png
*.local

# ── Rust / Tauri ──
vitality-nexus/src-tauri/target/
vitality-nexus/src-tauri/gen/schemas/
vitality-nexus/src-tauri/binaries/
vitality-nexus/src-tauri/vitality-backend*.exe

# ── Python / 백엔드 ──
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
*.pyc
backend/data/holdings.json
backend/data/watchlist.json
backend/data/vitality.db
backend/dist/
backend/build/
backend/*.spec
backend/sidecar_entry.py
backend/server*.log

# ── 개인 데이터 / 시크릿 ──
.env
*.env.local

# ── OS ──
Thumbs.db
.DS_Store

# 진단 로그 (런타임 생성)
backend/data/clientlog.txt
vitality-nexus/tauri-dev.log

# 출시 리뷰 보드 산출물 — 냉정한 내부 평가라 공개 저장소에 안 올림 (로컬 전용)
docs/reviews/

# ── 장 마감 콘텐츠 파이프라인(market-close) ──
# 코드·규격·대본은 넣고, 만들어진 것과 남의 것은 뺀다.
market-close/render/node_modules/
market-close/render/public/voice/
market-close/render/props_*.json
market-close/render/thumb_props_*.json
market-close/logs/
market-close/data/auth/
# 참고 채널 자막 — 남의 저작물이라 저장소에 넣지 않는다(jobs/collect_refs.py로 다시 받는다)
market-close/data/refs/
market-close/data/**/raw/
# 렌더 결과물 — jobs/run_day.py 로 다시 만든다
market-close/out/**/*.mp4
market-close/out/**/*.jpg
market-close/out/**/*.png
market-close/out/**/*.srt
market-close/out/**/*.mp3
market-close/out/**/voice/
market-close/out/**/frames/
# 합성 배경음 — jobs/make_bgm.py 가 다시 만든다
market-close/render/public/bgm/*.wav
__pycache__/
